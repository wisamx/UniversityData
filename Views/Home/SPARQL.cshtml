
@{
    ViewBag.Title = "SPARQL";
}

<h2>SPARQL</h2>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    <p>Enter your SPARQL query in the text box below:</p>
    @Html.TextArea("query", "SELECT ?d", 10, 50, null)

    <p>
        <input type="submit" value="Query" />
    </p>
}